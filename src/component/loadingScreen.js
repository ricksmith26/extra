import React from 'react';
import ReactLoading from 'react-loading';

const Loading = ({ type, color }) => (
  <div>
    <ReactLoading type={'cylon'} color={'#6395F2'} height={667} width={375} />
  </div>
);

export default Loading;
